# Use empty for Vite dev proxy to Laravel (same-origin /api)
VITE_API_URL=
# Laragon vhost (document root api/public). Use php artisan serve URL if not using Laragon.
VITE_API_PROXY_TARGET=http://prism.test/api
